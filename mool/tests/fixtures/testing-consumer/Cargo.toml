[package]
name = "mool-testing-consumer"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
mool = { path = "../../../", default-features = false, features = ["sqlite", "test-support"] }
