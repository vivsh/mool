[package]
name = "mool-testing-mysql-migrations-rejected"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
mool = { path = "../../../", default-features = false, features = ["mysql", "migrations", "test-support"] }
