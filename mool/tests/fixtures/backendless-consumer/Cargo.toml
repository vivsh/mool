[package]
name = "mool-backendless-consumer"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
mool = { path = "../../../", default-features = false, features = ["migrations"] }
